{
  "schemaVersion": 1,
  "id": "offlcersam_shipfoundry",
  "version": "0.1.0",
  "name": "ShipForge",
  "description": "Loads ship definitions from JSON files under <gameDir>/ships/<modName>/, so ships can be made as json data instead of Java/Mixin code.",
  "environment": "*",

  "mixins": [
    "shipfoundry.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=0.18.4",
    "java": ">=25",
    "sector-space": ">=0.5.9.6"
  }
}
